# Wisp Wallet download statistics

The exact per-version APK download report is being generated from GitHub Releases.

Only uploaded `.apk` assets are counted. Drafts, checksums, source archives, and non-APK files are excluded.
